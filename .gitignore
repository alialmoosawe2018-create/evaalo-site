# Dependencies
node_modules/
.pnp
.pnp.js

# Runtime data — candidate uploads (PII) must never enter git
uploads/

# Python tooling caches (avatar agents)
.venv/
__pycache__/
.pytest_cache/
.ruff_cache/

# Embedded review clone (separate git repo, not part of this tree)
evaalo-backend-prod-review/

# Root-level scratch artifacts from n8n/migration sessions
.tmp-*

# Testing
coverage/
*.log

# Production
dist/
build/
*.local

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# OS
Thumbs.db
desktop.ini

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Temporary files
tmp/
temp/
*.tmp

# Package manager locks (optional - uncomment if needed)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml


# Deploy scratch (embedded clone)
.deploy-tmp/

# Graphify knowledge-graph build output (local, regenerable via `graphify update .`)
graphify-out/

# Per-machine Claude Code settings: the hook commands are absolute paths to one
# developer's home directory, so committing this would break every other machine.
# Shared project config belongs in .claude/launch.json and CLAUDE.md, which ARE tracked.
.claude/settings.json
.claude/settings.local.json
