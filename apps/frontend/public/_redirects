# Cloudflare Pages — SPA fallback.
# Asset paths must come first so /assets/* is not rewritten to index.html
# (which would return text/html and break CSS/JS MIME types).
/assets/*  /assets/:splat  200
/*        /index.html     200
