# SPA fallback. It matches asset paths too, so a missing /assets/* file is answered
# with this HTML at status 200; `_redirects` cannot rewrite to a 404 to prevent that.
# `functions/assets/[[path]].js` catches it and returns a real 404 instead.
/*  /index.html  200
