# 📖 تعليمات تشغيل المشروع - خطوة بخطوة

## 🖥️ أين تضع الأوامر؟

الأوامر تُكتب في **Terminal** أو **Command Prompt** أو **PowerShell**

---

## 📝 الخطوات التفصيلية:

### 1️⃣ فتح Terminal/Command Prompt

**الطريقة 1: من File Explorer**
1. افتح مجلد `cursor-react`
2. اضغط `Shift + Right Click` (زر الماوس الأيمن + Shift)
3. اختر "Open PowerShell window here" أو "Open in Terminal"

**الطريقة 2: من Cursor/VS Code**
1. اضغط `Ctrl + ~` (أو View → Terminal)
2. Terminal سيفتح في أسفل الشاشة

**الطريقة 3: من قائمة Start**
1. ابحث عن "PowerShell" أو "Command Prompt"
2. افتحه
3. اكتب: `cd C:\Users\Alnaji-AliMD\.cursor\cursor-react`

---

### 2️⃣ تثبيت الحزم (مرة واحدة فقط)

في Terminal، اكتب:
```bash
npm install
```

ثم اضغط `Enter`

⏳ انتظر حتى ينتهي التثبيت (قد يستغرق 1-2 دقيقة)

---

### 3️⃣ تشغيل المشروع

في نفس Terminal، اكتب:
```bash
npm run dev
```

ثم اضغط `Enter`

✅ سترى رسالة مثل:
```
  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose
```

🌐 افتح المتصفح واذهب إلى: **http://localhost:3000**

---

### 4️⃣ إيقاف الخادم

**عندما تريد إيقاف الخادم:**

1. انقر داخل نافذة Terminal (حيث يعمل `npm run dev`)
2. اضغط على لوحة المفاتيح: **`Ctrl + C`**
   - **لا تكتب** `Ctrl + C` في Terminal
   - **بل اضغط** على زر `Ctrl` وزر `C` معاً في لوحة المفاتيح
3. سيتم إيقاف الخادم

---

### 5️⃣ إعادة التشغيل

بعد إيقاف الخادم، لتشغيله مرة أخرى:

في نفس Terminal، اكتب:
```bash
npm run dev
```

ثم اضغط `Enter`

---

### 6️⃣ بناء المشروع للإنتاج

عندما تريد إنشاء نسخة نهائية للمشروع:

في Terminal، اكتب:
```bash
npm run build
```

ثم اضغط `Enter`

⏳ انتظر حتى ينتهي البناء
✅ سيتم إنشاء مجلد `dist` يحتوي على الملفات النهائية

---

### 7️⃣ معاينة البناء

لرؤية كيف سيبدو المشروع بعد البناء:

في Terminal، اكتب:
```bash
npm run preview
```

ثم اضغط `Enter`

🌐 سيتم فتح نسخة الإنتاج على: **http://localhost:4173**

---

## 📸 مثال على Terminal:

```
PS C:\Users\Alnaji-AliMD\.cursor\cursor-react> npm run dev

  VITE v5.0.8  ready in 500 ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: use --host to expose

  ready in 500 ms.
```

**هنا الخادم يعمل!** 
- لا تغلق Terminal
- اتركه يعمل
- افتح المتصفح واذهب إلى http://localhost:3000

---

## ⚠️ ملاحظات مهمة:

1. **Terminal يجب أن يبقى مفتوحاً** أثناء تشغيل `npm run dev`
2. **لا تغلق Terminal** إذا أردت أن يعمل الموقع
3. **Ctrl + C** = إيقاف الخادم (اضغط الأزرار، لا تكتبها)
4. **كل أمر** يجب أن يكون في سطر منفصل
5. **اضغط Enter** بعد كل أمر

---

## 🎯 ملخص سريع:

```
1. افتح Terminal في مجلد cursor-react
2. npm install          ← تثبيت الحزم (مرة واحدة)
3. npm run dev          ← تشغيل المشروع
4. افتح المتصفح → http://localhost:3000
5. Ctrl + C             ← إيقاف الخادم (عند الحاجة)
```

---

## ❓ مشاكل شائعة:

**المشكلة:** "npm is not recognized"
**الحل:** يجب تثبيت Node.js من https://nodejs.org

**المشكلة:** "Port 3000 is already in use"
**الحل:** الخادم يعمل بالفعل، افتح المتصفح مباشرة

**المشكلة:** "Cannot find module"
**الحل:** قم بتشغيل `npm install` أولاً



