# Backend - Vapi Voice Agent

## 📋 الوصف
Backend للتطبيق الخاص بالوكيل الصوتي باستخدام Vapi AI.

## 🚀 التثبيت والتشغيل

### 1. تثبيت التبعيات
```bash
cd backend
npm install
```

### 2. إعداد ملف البيئة
```bash
# انسخ ملف env.example إلى .env
copy env.example .env

# أو في Linux/Mac:
cp env.example .env
```

ثم افتح ملف `.env` وأضف:
- `VAPI_API_KEY`: مفتاح API من Vapi
- `PORT`: رقم المنفذ (افتراضي: 5000)
- `FRONTEND_URL`: رابط Frontend (افتراضي: http://localhost:3000)

### 3. التشغيل في وضع التطوير
```bash
npm run dev
```

السيرفر سيعمل على: `http://localhost:5000`

### 4. بناء المشروع
```bash
npm run build
```

### 5. التشغيل في وضع الإنتاج
```bash
npm start
```

## 📁 البنية

```
backend/
├── src/
│   ├── server.ts          # السيرفر الرئيسي
│   ├── assistant.ts       # إدارة المساعدين
│   ├── webhook.ts         # معالجة Webhooks
│   └── routes/            # مسارات API
├── dist/                  # ملفات البناء (يتم إنشاؤها)
├── package.json
├── tsconfig.json
└── .env                   # ملف البيئة (لا يتم رفعه)
```

## 🔧 الأوامر المتاحة

- `npm run dev` - تشغيل في وضع التطوير مع auto-reload
- `npm run build` - بناء المشروع
- `npm start` - تشغيل في وضع الإنتاج
- `npm run type-check` - فحص أنواع TypeScript

## 📝 ملاحظات

- السيرفر يستخدم TypeScript
- يتم استخدام `tsx` للتشغيل المباشر في التطوير
- ملفات `.env` لا يتم رفعها على Git (موجودة في .gitignore)

