# إعداد n8n من حاسوب آخر

## السيناريو: n8n يعمل على حاسوب آخر

### الحل 1: n8n على نفس الشبكة المحلية

إذا كان n8n يعمل على حاسوب آخر في نفس الشبكة (Wi-Fi أو LAN):

**استخدم عنوان IP المحلي:**
```
http://192.168.1.104:5000/webhook/n8n
```

**خطوات التحقق:**

1. **تأكد من أن السيرفر يستمع على جميع الواجهات:**
   - السيرفر يجب أن يستمع على `0.0.0.0` وليس فقط `localhost`
   - تحقق من أن السيرفر يعمل على المنفذ 5000

2. **افتح Firewall للمنفذ 5000:**
   ```powershell
   # في PowerShell (كمسؤول)
   New-NetFirewallRule -DisplayName "Allow Port 5000" -Direction Inbound -LocalPort 5000 -Protocol TCP -Action Allow
   ```

3. **اختبر الاتصال من الحاسوب الآخر:**
   ```bash
   # من الحاسوب الذي يعمل عليه n8n
   curl http://192.168.1.104:5000/health
   ```

### الحل 2: n8n على الإنترنت (سيرفر خارجي)

إذا كان n8n يعمل على سيرفر خارجي أو على الإنترنت:

**استخدم ngrok:**

1. **تثبيت ngrok:**
   - حمّل من: https://ngrok.com/download
   - أو استخدم: `npm install -g ngrok`

2. **إنشاء حساب مجاني:**
   - سجل في: https://dashboard.ngrok.com/signup
   - احصل على authtoken

3. **إعداد ngrok:**
   ```bash
   ngrok config add-authtoken YOUR_AUTH_TOKEN
   ```

4. **تشغيل ngrok:**
   ```bash
   ngrok http 5000
   ```

5. **ستحصل على رابط مثل:**
   ```
   Forwarding  https://abc123.ngrok.io -> http://localhost:5000
   ```

6. **استخدم في n8n:**
   ```
   https://abc123.ngrok.io/webhook/n8n
   ```

**ملاحظة:** في النسخة المجانية، الرابط يتغير في كل مرة. للحصول على رابط ثابت، تحتاج إلى خطة مدفوعة.

### الحل 3: استخدام عنوان IP العام

إذا كان لديك عنوان IP عام ثابت:

1. **اعرف عنوان IP العام:**
   ```bash
   curl ifconfig.me
   ```

2. **افتح Port Forwarding في Router:**
   - افتح إعدادات Router
   - أضف Port Forwarding: المنفذ 5000 → 192.168.1.104:5000

3. **استخدم في n8n:**
   ```
   http://YOUR_PUBLIC_IP:5000/webhook/n8n
   ```

## التحقق من الإعداد

### 1. تحقق من أن السيرفر يستمع على جميع الواجهات:

في `server.ts`، تأكد من:
```typescript
app.listen(PORT, '0.0.0.0', () => {
    console.log(`🚀 Server is running on http://0.0.0.0:${PORT}`);
});
```

### 2. اختبر الاتصال محلياً:

```bash
curl http://localhost:5000/health
```

### 3. اختبر الاتصال من حاسوب آخر:

```bash
# من الحاسوب الذي يعمل عليه n8n
curl http://192.168.1.104:5000/health
```

### 4. اختبر webhook من n8n:

استخدم **HTTP Request** node في n8n:
- Method: `POST`
- URL: `http://192.168.1.104:5000/webhook/n8n`
- Body: JSON مع `candidateId`

## استكشاف الأخطاء

### خطأ: "Connection refused"
- تأكد من أن السيرفر يعمل
- تحقق من Firewall
- تأكد من أن السيرفر يستمع على `0.0.0.0`

### خطأ: "Timeout"
- تحقق من أن الحاسوبين على نفس الشبكة
- تحقق من Firewall
- جرب ping: `ping 192.168.1.104`

### خطأ: "CORS error"
- تأكد من إعداد CORS في السيرفر
- أضف عنوان n8n إلى قائمة المسموح بها

## الأمان

⚠️ **تحذير:** فتح المنفذ 5000 للوصول الخارجي قد يشكل خطراً أمنياً.

**نصائح:**
1. استخدم HTTPS في الإنتاج
2. أضف authentication للـ webhook
3. استخدم VPN للاتصال الآمن
4. قيد الوصول بعناوين IP معينة


